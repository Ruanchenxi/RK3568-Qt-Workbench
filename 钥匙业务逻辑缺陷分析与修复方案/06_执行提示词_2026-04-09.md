# 执行提示词

以下内容用于在另一个会话中执行修复方案。直接复制整段发给目标会话即可。

---

你现在是一个代码执行 agent。你的任务是按照下面的修复方案，逐条修改代码。

## 核心要求

1. **严格按照方案执行**，不要自行发散或"优化"
2. **每完成一个 C 编号的修复，立即报告改了什么**
3. **不要改方案没提到的代码**
4. **回传自动清理的收口逻辑（`AutoReturnCleanup` 分支）必须逐字保留原有代码**，不得简化、重排或"改进"
5. 改完后给出一个**完整的改动清单**

## 修复方案文档

请先阅读以下两个文档，建立上下文：

1. `D:\Desktop\QTtest\RK3568\钥匙业务逻辑缺陷分析与修复方案\04_合并问题清单_2026-04-09.md`
2. `D:\Desktop\QTtest\RK3568\钥匙业务逻辑缺陷分析与修复方案\05_修复执行方案_2026-04-09.md`

## 执行顺序

严格按以下顺序执行，不要跳步：

### 步骤 1：C1 — `onDeleteClicked()` 补记 sessionId

在 `KeyManageController.cpp` 的 `onDeleteClicked()` 函数中：

找到：
```cpp
m_pendingDeleteAllowsRetransfer = mappedTicket.valid
        && (mappedTicket.returnState.isEmpty() || mappedTicket.returnState == QLatin1String("idle"));

CommandRequest req;
```

在两者之间加两行：
```cpp
m_pendingDeleteSessionId = m_keySessionId;
m_pendingDeleteRetryCount = 0;
```

**完成后报告。**

### 步骤 2：C5 — `tryAutoCleanupReturnedTicket()` 增加互斥检查

在 `KeyManageController.cpp` 的 `tryAutoCleanupReturnedTicket()` 函数中：

**2a** — 在 `if (!m_pendingDeletedSystemTicketId.isEmpty()) { return; }` 之后加：
```cpp
if (!m_pendingCancelSystemTicketId.isEmpty()) {
    return;
}
```

**2b** — 在循环内，在 `if (!ticket.valid) { continue; }` 之后（注意：当前代码里这一行可能不存在，需要在 `returnState` 检查之前加），加：
```cpp
if (!ticket.cancelState.isEmpty() && ticket.cancelState != QLatin1String("none")) {
    continue;
}
```

**完成后报告。**

### 步骤 3：C3 — 加 DeleteOrigin 标记

**3a** — 在 `KeyManageController.h` 的 private 成员区域，加：
```cpp
enum class DeleteOrigin { None, ManualDelete, AutoReturnCleanup };
DeleteOrigin m_pendingDeleteOrigin = DeleteOrigin::None;
```

**3b** — 在 `onDeleteClicked()` 中（步骤 1 加的两行之后），加：
```cpp
m_pendingDeleteOrigin = DeleteOrigin::ManualDelete;
```

**3c** — 在 `tryAutoCleanupReturnedTicket()` 中，`m_pendingDeleteSessionId = m_keySessionId;` 之后加：
```cpp
m_pendingDeleteOrigin = DeleteOrigin::AutoReturnCleanup;
```

**3d** — 在 `markReturnDeletePending()` 中，`m_pendingDeleteRetryCount = 0;` 之后加：
```cpp
m_pendingDeleteOrigin = DeleteOrigin::AutoReturnCleanup;
```

**3e** — 修改 `finalizePendingReturnDelete()` 的成功分支（`if (!stillExists && !sessionUnknown)` 块）。

把**当前代码**：
```cpp
if (!stillExists && !sessionUnknown) {
    if (m_pendingDeleteAllowsRetransfer) {
        m_ticketStore->markKeyTaskDeleted(m_pendingDeletedSystemTicketId);
        emit statusMessage(QStringLiteral("钥匙任务已删除，可再次传票"));
    } else {
        const SystemTicketDto ticket = m_ticketStore->ticketById(m_pendingDeletedSystemTicketId);
        if (ticket.valid && (ticket.returnState == QLatin1String("return-delete-verifying")
                             || ticket.returnState == QLatin1String("return-delete-pending")
                             || ticket.returnState == QLatin1String("return-upload-success"))) {
            m_ticketStore->updateReturnState(m_pendingDeletedSystemTicketId,
                                             QStringLiteral("return-delete-success"));
            emit statusMessage(QStringLiteral("回传完成，钥匙任务已清理：%1")
                                   .arg(m_pendingDeletedSystemTicketId));
            emit workbenchRefreshRequested();
        } else {
            emit statusMessage(QStringLiteral("钥匙任务已删除"));
        }
    }
    m_pendingDeletedSystemTicketId.clear();
    m_pendingDeletedKeyTaskRaw.clear();
    m_pendingDeleteAllowsRetransfer = false;
    m_pendingDeleteSessionId = 0;
    m_pendingDeleteRetryCount = 0;
}
```

替换为：
```cpp
if (!stillExists && !sessionUnknown) {
    if (m_pendingDeleteOrigin == DeleteOrigin::ManualDelete) {
        // 手工删除收口：标 key-cleared + 调 termination + 移除本地票
        m_ticketStore->markKeyTaskDeleted(m_pendingDeletedSystemTicketId);
        const SystemTicketDto ticket = m_ticketStore->ticketById(m_pendingDeletedSystemTicketId);
        if (ticket.valid) {
            callTermination(m_pendingDeletedSystemTicketId);
            m_ticketStore->removeTicket(m_pendingDeletedSystemTicketId);
            emit statusMessage(QStringLiteral("钥匙任务已删除并已终止系统票：%1")
                                   .arg(m_pendingDeletedSystemTicketId));
        } else {
            emit statusMessage(QStringLiteral("钥匙残留任务已清理（本地记录已过期）：%1")
                                   .arg(m_pendingDeletedSystemTicketId));
        }
    } else {
        // 回传清理收口 — 原有逻辑完全不变
        if (m_pendingDeleteAllowsRetransfer) {
            m_ticketStore->markKeyTaskDeleted(m_pendingDeletedSystemTicketId);
            emit statusMessage(QStringLiteral("钥匙任务已删除，可再次传票"));
        } else {
            const SystemTicketDto ticket = m_ticketStore->ticketById(m_pendingDeletedSystemTicketId);
            if (ticket.valid && (ticket.returnState == QLatin1String("return-delete-verifying")
                                 || ticket.returnState == QLatin1String("return-delete-pending")
                                 || ticket.returnState == QLatin1String("return-upload-success"))) {
                m_ticketStore->updateReturnState(m_pendingDeletedSystemTicketId,
                                                 QStringLiteral("return-delete-success"));
                emit statusMessage(QStringLiteral("回传完成，钥匙任务已清理：%1")
                                       .arg(m_pendingDeletedSystemTicketId));
                emit workbenchRefreshRequested();
            } else {
                emit statusMessage(QStringLiteral("钥匙任务已删除"));
            }
        }
    }
    m_pendingDeletedSystemTicketId.clear();
    m_pendingDeletedKeyTaskRaw.clear();
    m_pendingDeleteAllowsRetransfer = false;
    m_pendingDeleteSessionId = 0;
    m_pendingDeleteRetryCount = 0;
    m_pendingDeleteOrigin = DeleteOrigin::None;
}
```

**3f** — 在所有清理 pending delete 状态的地方，加 `m_pendingDeleteOrigin = DeleteOrigin::None;`：
- `retryPendingReturnDelete` 的失败收口（`m_pendingDeleteRetryCount >= 3` 之后的清理块）
- `onDeleteClicked` 开头的 `m_pendingDeletedSystemTicketId.clear();` 之后

**完成后报告。**

### 步骤 4（可选）：C9 — 撤销日志过期清理

如果前面步骤都完成且编译通过，继续做 C9。具体代码见修复方案文档 `05_修复执行方案_2026-04-09.md` 的 C9 部分。

**完成后报告。**

### 搁置不做的步骤

以下步骤本轮**不执行**：

- **C2（撤销链 session 校验）**：`m_keySessionId` 是串口会话编号，不是钥匙物理身份。同一把钥匙拔插后 session 就变，用它做"原钥匙"判断会误拒合法操作。需要协议层提供钥匙 UID 后再实施。
- **C4（传票反向对账）**：依赖 C2 的 `transferSessionId`，同因搁置。
- **C8（手工删除后调 termination）**：依赖 C3 + 需确认 termination 接口格式，延后单独做。
- **C6（孤儿恢复增强）**：部分已修，剩余改动可延后。
- **C7（按钮权限控制）**：需要先确认登录模块的角色机制。

---

## 完成后的输出要求

全部做完后，请输出一个**改动清单**，格式如下：

```
## 改动清单

### C1
- 文件：KeyManageController.cpp
- 函数：onDeleteClicked()
- 改动：加了 X 行
- 行号：约 line XXX

### C5
...

### C3
...

### 编译结果
- Windows 编译：通过/未通过
- 如有编译错误，列出错误信息
```
