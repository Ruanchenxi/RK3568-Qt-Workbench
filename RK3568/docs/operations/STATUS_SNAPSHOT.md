# 当前状态快照（STATUS SNAPSHOT）

Status: Active  
Owner: 项目维护者  
Last Updated: 2026-04-03
适用范围：快速说明“项目当前做到哪里、哪些链路可用、哪些仍待真机验证”。  
不适用范围：不替代协议逐字节参考，不替代完整架构文档。  
说明：本文件按历史增量持续追加，早期条目保留原编号，可能存在重复号或预留号；新增条目以最新事实为准。  

## 1. 当前阶段一句话

RK3568 主程序当前已完成：工作台 JSON 进入主程序 -> 系统票入池 -> 手动/自动传票 -> 按任务单号独立回传 -> `cancelTicket` 主链 -> TicketStore 持久化与启动恢复加载。当前重点已转向：专项真机样本补齐、旧文档口径收口、以及多钥匙前的数据/状态边界确认。

## 2. 已完成

1. 主程序内已接入本地 HTTP 接收能力：
   - 默认 `127.0.0.1:8888`
   - 默认路径 `/ywticket/WebApi/transTicket`
   - 支持 `OPTIONS` 预检与跨域
   - 启动日志当前会打印真实配置的 `host / port / path`
2. 接收到的 JSON 会：
   - 落盘到 `logs/http_ticket_*.json`
   - 进入系统票池
   - 展示到钥匙管理页 `HTTP服务端报文`
   - 展示到钥匙管理页 `系统票数据`
   - 只有在业务真正入池成功时才返回 `success=true`
3. 当前本地 HTTP 接票入口的错误语义已收紧：
   - 错路径：`404`
   - 错方法：`405`
   - 请求行非法 / `Content-Length` 缺失或非法 / JSON 无法入池：`400`
4. 主程序内已经移植传票协议纯逻辑：
   - `JSON -> payload`
   - `payload -> frame(s)`
   - 单帧 / 多帧
   - ACK 驱动续发
5. 手动传票入口已改成：
   - 选中系统票
   - 点“传票”
   - 走主程序钥匙主链
6. 自动传票逻辑已落地，并已开启默认值：
   - `ticket/autoTransferEnabled=true`
   - 仍受串口连接、钥匙在位、钥匙稳定、`sessionReady` 共同约束
7. 同一 `taskId` 在 `sending/success` 状态下再次触发时，当前默认忽略重复下发
   - 业务忽略并不等于“盲目 success”
   - 只有真实入池成功时才返回 `success=true`
8. 若钥匙中的对应任务已被手动删除（DEL 成功），系统票会进入 `key-cleared`，允许后续再次传票
9. 当前真机联调已确认：
   - 工作台 JSON 接收正常
   - 系统票入池正常
   - 手动传票主链可进入正式发送入口
   - 自动传票主链已有板端样本
10. 单帧回传主链已接入：
   - `Q_TASK(status=0x02) -> I_TASK_LOG -> ACK(5A 05 00 00) -> UP_TASK_LOG -> HTTP回传 -> DEL`
11. `HTTP客户端报文` 页已能显示真实回传请求与响应
12. 本地 `localhost` 后端可接受 `/finish-step-batch` 的 `POST application/json`
13. 原产品与当前主程序都已证实：
   - 工作台任务中的 `rfid` 与回传日志中的 `rfid` 不是同一个语义
   - 单步停电 / 送电的回传 `rfid` 可能不同
14. 多帧 `UP_TASK_LOG` 回传代码已实现，并已通过 replay 场景验证
15. 钥匙放回并重新 ready 后，主程序会自动触发一次 `Q_TASK`，不再依赖手工点“读取钥匙列表”才进入回传
16. 手工 `初始化(INIT 0x02)` 已接入主程序，并已完成真机分帧发送与 ACK 验证
17. 手工 `下载 RFID(DN_RFID 0x1A)` 已接入主程序，并已完成真机发送与 ACK 验证
18. 接入 `INIT / 下载 RFID` 后，主程序自动回传主链零回归通过：
   - `ready -> Q_TASK -> I_TASK_LOG -> UP_TASK_LOG -> HTTP回传 -> DEL`
   - 过程中不会自动插入 `INIT / DN_RFID`
19. 顶部与底部已按“设备态 / 业务态”分工：
   - 顶部只显示设备健康态短状态词
   - 底部状态栏只显示当前业务动作或最近一次事件
20. `当前配置座号` 当前显示的是已配置站号/座号，不是底座自动探测出的物理座号
21. `删除钥匙票` 当前按“钥匙票表选中项”执行；未选中时会直接提示，不再默认删除第一条任务
22. 当底座抖动导致本地主程序票状态仍停在 `sending/auto-pending/failed`，但 `Q_TASK` 已确认钥匙中存在该任务时，主程序会自动把本地 `transferState` 对账纠正为 `success`
23. 当再次放下钥匙后，若自动 `Q_TASK` 成功确认“钥匙中没有任务”，而本地系统票仍停在 `sending/failed`，主程序会把它重新放回 `auto-pending` 并自动再试一次传票
24. 若 HTTP 回传已成功，但随后 `Q_TASK` 仍读到钥匙中残留的 `status=0x02` 任务，主程序会自动继续执行 `DEL` 清理，而不是停留在“已回传，待人工处理”
25. 串口重试定时器当前已增加 stale-timeout 防护：若响应已到并清除了 in-flight，再晚到的旧 timeout 不再继续触发错误重试
26. 当旧的 `Q_TASK / I_TASK_LOG` 重复回包晚到，而当前已有其他命令在飞或日志上传已进入下一阶段时，主程序会忽略这些重复回包
27. 自动回传当前已改为更贴近原产品的编排：
   - 检测到 `status=0x02` 后，先补一次 `SET_COM`
   - 待 `SET_COM ACK` 到达后再发 `I_TASK_LOG`
28. Linux 板端启动路径已补图形诊断并收缩为保守模式
29. 传票主链当前也已补 `SET_COM -> ACK -> TICKET` 前置握手，用于提升空闲后一发首包成功率
30. TicketStore 当前已持久化：
   - 票池执行态落盘到 `logs/ticket_store.json`
   - 启动时在 `KeyManageController::start()` 中加载
   - 已完成票按 7 天策略清理
31. TicketStore 当前支持孤儿恢复：
   - `cancelTicket` 发现本地票不存在但钥匙中有任务时，会先建最小孤儿记录
   - `TasksUpdated` 对账时若发现钥匙中有任务而本地无记录，也会自动补孤儿票
32. 2026-04-01 按任务单号独立回传已代码级落地：
   - 手工回传只看目标任务是否完成
   - 自动回传从已完成任务里选一条发起
   - 当前仍严格串行，不支持并发回传
33. 2026-04-03 `cancelTicket` 主链已落地：
   - 本地 HTTP 入口已支持 `/ywticket/WebApi/cancelTicket`
   - `cancelTicket` 当前已区分：
     - 本地无票
     - 已完成闭环无需撤销
     - 已在回传清理链中无需重复撤销
     - 未下发且确认不在钥匙中时直接移除
     - 其他情况受理为 `cancel-accepted/cancel-pending`
   - 当前已补：
     - `cancelState`
     - 最小撤销记录 `logs/cancel_ticket_journal.json`
     - 自动传票 / 自动回传 / 自动恢复对撤销票的冻结
     - `Q_TASK -> DEL -> Q_TASK verify` 的撤销删除闭环
34. 2026-04-03 “传票过程中拔钥匙”当前已按业务失败处理：
   - 本地标记 `failed`
   - 不记 `success`
   - 后续允许重新传票

## 3. 当前页面能力

### 钥匙管理页

1. `HTTP服务端报文`：可看到收到的 JSON 原文。  
2. `系统票数据`：可看到系统票列表。  
3. 左侧“当前选中票据”：跟随系统票选中项联动。  
4. `传票`：已接入正式发送入口。  
5. `回传`：当前按任务单号独立门禁。  
6. `撤销`：当前走 `cancelTicket` 受理与删除验证链。  

### 串口报文页

1. 仍由原有钥匙串口主链提供。  
2. 旧日志链没有被替换成平行实现。  

## 4. 已落地但尚未完全真机确认

1. TicketStore 持久化的完整专项样本仍待继续补齐：
   - 重启恢复闭环
   - 损坏文件降级
   - 7 天清理现场样本
2. orphan recovery 当前代码已落地，但板端专项样本仍待补
3. 自动独立回传当前代码已落地，但自动路径专项样本仍待继续补齐
4. `cancelTicket` 当前正常主链已有样本，但以下专项仍待补：
   - `cancel-pending -> cancel-executing -> cancel-done` 完整板端样本
   - 程序重启后最小撤销记录恢复样本
5. 工作台白屏恢复专项板端验证仍待继续
6. replay 回归在当前会话内尚未真正统一执行并核对输出

## 5. 当前明确未做 / 刻意延后

1. 多钥匙场景下撤销链的真实 slot 绑定与现场验证。  
2. 将 `INIT / 下载 RFID` 改造成 ready 自动前置链。  
3. 多钥匙真实调度当前只预埋 `residentSlotId / residentKeyId`，尚未进入实现。  

## 6. 下一步重点

1. 继续补 TicketStore 持久化 / orphan recovery / cancel recovery 的专项真机样本。  
2. 继续补自动独立回传的专项样本。  
3. 统一清理旧文档中“整把钥匙全部完成才能回传”“TicketStore 无持久化”等历史口径。  


Status: Active  
Owner: 项目维护者  
Last Updated: 2026-03-30
适用范围：快速说明“项目当前做到哪里、哪些链路可用、哪些仍待真机验证”。  
不适用范围：不替代协议逐字节参考，不替代完整架构文档。  
说明：本文件按历史增量持续追加，早期条目保留原编号，可能存在重复号或预留号；新增条目以最新事实为准。  

## 1. 当前阶段一句话

RK3568 主程序已完成“工作台 JSON 进入主程序 -> 系统票入池 -> 钥匙管理页展示 -> 手动/自动传票 -> 单帧回传日志 -> HTTP回传 -> DEL”主链；手工 `INIT(0x02)` 与手工 `DN_RFID(0x1A)` 也已接入并通过真机 ACK 验证，当前重点已转向 `送电样本 / 真实多帧回传样本 / 后续业务收口`。

## 2. 已完成

1. 主程序内已接入本地 HTTP 接收能力：
   - 默认 `127.0.0.1:8888`
   - 默认路径 `/ywticket/WebApi/transTicket`
   - 支持 `OPTIONS` 预检与跨域
   - 启动日志当前会打印真实配置的 `host / port / path`
2. 接收到的 JSON 会：
   - 落盘到 `logs/http_ticket_*.json`
   - 进入系统票池
   - 展示到钥匙管理页 `HTTP服务端报文`
   - 展示到钥匙管理页 `系统票数据`
   - 只有在业务真正入池成功时才返回 `success=true`
3. 当前本地 HTTP 接票入口的错误语义已收紧：
   - 错路径：`404`
   - 错方法：`405`
   - 请求行非法 / `Content-Length` 缺失或非法 / JSON 无法入池：`400`
4. 主程序内已经移植传票协议纯逻辑：
   - `JSON -> payload`
   - `payload -> frame(s)`
   - 单帧 / 多帧
   - ACK 驱动续发
5. 手动传票入口已改成：
   - 选中系统票
   - 点“传票”
   - 走主程序钥匙主链
6. 自动传票逻辑已落地，并已开启默认值：
   - `ticket/autoTransferEnabled=true`
   - 仍受串口连接、钥匙在位、钥匙稳定、`sessionReady` 共同约束
7. 同一 `taskId` 在 `sending/success` 状态下再次触发时，当前默认忽略重复下发
   - HTTP 当前仍返回 `success=true`
   - 但 `msg` 会区分“duplicate ignored / already sending / auto transfer disabled”等业务语义
8. 若钥匙中的对应任务已被手动删除（DEL 成功），系统票会进入 `key-cleared`，允许后续再次传票
9. 当前真机联调已确认：
   - 工作台 JSON 接收正常
   - 系统票入池正常
   - 手动传票主链可进入正式发送入口
   - 自动传票主链已开始真机验证
10. 单帧回传主链已接入：
   - `Q_TASK(status=0x02) -> I_TASK_LOG -> ACK(5A 05 00 00) -> UP_TASK_LOG -> HTTP回传 -> DEL`
11. `HTTP客户端报文` 页已能显示真实回传请求与响应
12. 本地 `localhost` 后端可接受 `/finish-step-batch` 的 `POST application/json`
13. 原产品与当前主程序都已证实：
   - 工作台任务中的 `rfid` 与回传日志中的 `rfid` 不是同一个语义
   - 单步停电 / 送电的回传 `rfid` 可能不同
13. 多帧 `UP_TASK_LOG` 回传代码已实现，并已通过 replay 场景验证
14. 钥匙放回并重新 ready 后，主程序会自动触发一次 `Q_TASK`，不再依赖手工点“读取钥匙列表”才进入回传
15. 手工 `初始化(INIT 0x02)` 已接入主程序，并已完成真机分帧发送与 ACK 验证
16. 手工 `下载 RFID(DN_RFID 0x1A)` 已接入主程序，并已完成真机发送与 ACK 验证
17. 接入 `INIT / 下载 RFID` 后，主程序自动回传主链零回归通过：
   - `ready -> Q_TASK -> I_TASK_LOG -> UP_TASK_LOG -> HTTP回传 -> DEL`
   - 过程中不会自动插入 `INIT / DN_RFID`
18. 钥匙管理页顶部状态已收口为分层显示：
   - 通讯: 断开
   - 通讯: 待钥匙
   - 通讯: 稳定中
   - 通讯: 待握手
   - 通讯: 待确认（当前插钥匙会话中尚未确认过一次真实业务响应）
   - 通讯: 恢复中（此前已确认过业务通讯，但当前正在自动恢复）
   - 通讯: 钥匙已就绪（需拿到一次真实业务响应确认）
   - 顶部只展示短状态词，详细原因放到底部状态栏/串口报文
19. 顶部与底部已按“设备态 / 业务态”分工：
   - 顶部只显示设备健康态短状态词
   - 底部状态栏只显示当前业务动作或最近一次事件
   - `钥匙已稳定，等待通讯确认`、`钥匙已就绪，可发送 Q_TASK/DEL` 这类 steady-state 提示不再覆盖底部状态栏
20. `当前配置座号` 当前显示的是已配置站号/座号，不是底座自动探测出的物理座号
21. `删除钥匙票` 当前按“钥匙票表选中项”执行；未选中时会直接提示，不再默认删除第一条任务
22. 多步任务未全部完成就放回钥匙时，当前会明确提示“任务未全部完成，暂不回传，请完成全部步骤后再放回钥匙”
23. 当底座抖动导致本地主程序票状态仍停在 `sending/auto-pending/failed`，但 `Q_TASK` 已确认钥匙中存在该任务时，主程序会自动把本地 `transferState` 对账纠正为 `success`
24. 当再次放下钥匙后，若自动 `Q_TASK` 成功确认“钥匙中没有任务”，而本地系统票仍停在 `sending/failed`，主程序会把它重新放回 `auto-pending` 并自动再试一次传票
25. 若 HTTP 回传已成功，但随后 `Q_TASK` 仍读到钥匙中残留的 `status=0x02` 任务，主程序会自动继续执行 `DEL` 清理，而不是停留在“已回传，待人工处理”
26. 串口重试定时器当前已增加 stale-timeout 防护：若响应已到并清除了 in-flight，再晚到的旧 timeout 不再继续触发错误重试，避免把已成功的链路重新打回“恢复中/待确认”
27. 当旧的 `Q_TASK` / `I_TASK_LOG` 重复回包晚到，而当前已有其他命令在飞或日志上传已进入下一阶段时，主程序会忽略这些重复回包，避免旧包把当前传票/回传流程打乱
28. 自动回传当前已改为更贴近原产品的编排：
   - 检测到 `status=0x02` 后，不是直接发 `I_TASK_LOG`
   - 而是先补一次 `SET_COM`
   - 待 `SET_COM ACK` 到达后再发 `I_TASK_LOG`
   - 用于提升差底座/差接触条件下的回传稳定性
29. Linux 板端启动路径已补图形诊断并收缩为保守模式：
   - 默认 `RK3568_LINUX_GL_MODE=auto`，不再强制 `QT_XCB_GL_INTEGRATION=xcb_egl`
   - 仅在显式设置 `RK3568_LINUX_GL_MODE=egl/gles/xcb_egl_gles` 时，才强制 `xcb_egl + AA_UseOpenGLES`
   - 可用 `RK3568_LINUX_GL_MODE=software` 显式切到软件 OpenGL
   - 启动日志会输出 `RK3568_LINUX_GL_MODE / QT_QPA_PLATFORM / QT_XCB_GL_INTEGRATION / QT_OPENGL / QTWEBENGINE_CHROMIUM_FLAGS / AA_UseOpenGLES`
30. 板端事件循环已开始减负：
   - 工作台页改为首次显示时再创建 `QWebEngineView`
   - 隐藏日志页默认不立即落日志控件
   - 钥匙管理页的串口日志表 / HTTP 文本 / 重型表格改为合批刷新
31. 回传状态已拆细为：
   - `return-upload-success`
   - `return-delete-pending`
   - `return-delete-success`
   当前不再把“HTTP 上传成功”直接等同于“钥匙任务已清理成功”
32. 板端 HTTP 回传已加保守收口：
   - 使用 `QTimer + reply->abort()` 做超时控制
   - 2xx 但非 JSON object 不再默认视为成功
33. 2026-03-13 RK3588 板端补充验证已确认：
   - 默认保守图形模式下，主程序可正常启动
   - 真机已再次完成 `传票 -> 钥匙取走/放回 -> 自动回传 -> DEL -> Q_TASK(count=0)` 收口
   - 当前仍可见个别 `SET_COM` 重试后才收到 ACK，说明底座/板端时序压力仍在，但主链已不再卡死
34. 2026-03-13 晚新增回传业务门禁：
   - 自动回传与手工回传都不再允许“只完成一条就先回传一条”
   - 当前规则改为：钥匙中的相关任务必须全部完成后，才允许开始回传
   - 只要钥匙里仍有任意未完成任务，当前统一提示“暂不回传，请全部任务完成后再放回钥匙”
35. 2026-03-16 真机补充验证已确认：
   - “两条任务一条完成一条未完成”场景下，总门禁生效：
     - 不会提前回传
     - 手工回传按钮为灰色
   - “两条任务都完成”场景下，主程序可按“逐条回传、逐条 DEL”的方式自动收口
   - 典型链路可表现为：
     - 第一次 `Q_TASK resp: count=2`
     - 第一条回传并 `DEL`
     - 再次 `Q_TASK resp: count=1`
     - 第二条回传并 `DEL`
     - 最终 `Q_TASK resp: count=0`
36. 2026-03-16 本地补充验证已确认：
   - Windows 本地 `jom` 增量编译通过
   - 相关改动文件 `KeyManageController.cpp` 与 `keymanagepage.cpp` 已完成重新编译并成功链接 `debug/RK3568.exe`
37. 2026-03-16 登录页补充能力已接入：
   - 账号输入框右侧新增 `选择` 按钮
   - 当前可通过 `POST /list-account` 获取账号列表并弹窗选择
   - 该能力是登录辅助链，不改变原有“手输账号 + 密码登录”主链
38. 2026-03-17 自定义 QWidget 键盘主线已正式并入主程序：
   - 新增 `src/features/keyboard/` 模块
   - 按 `domain / application / ui` 分层组织
   - 旧 Qt Virtual Keyboard 过渡路径默认已切为 `none`
39. 当前键盘第一阶段已支持：
   - 登录页
   - 系统设置页
   - `QLineEdit`
   - `QComboBox::lineEdit()` 基础目标识别
40. 当前键盘第一阶段已具备：
   - 普通键盘
   - 数字键盘
   - URL 键盘
   - 模式切换闭环
   - 键盘打开状态下切字段自动换模式
   - 页面级尺寸策略
   - 固定候选栏中文输入最小闭环
41. 当前系统设置页输入策略已收口为更偏设备产品形态：
   - `keySerialCombo / cardSerialCombo` 改为自动识别 + 下拉选择
   - `baudRate / dataBits` 改为固定协议参数展示，不再允许编辑
42. 工作台页特例接入当前仍未开始做正式代码并入：
   - 当前不纳入自定义 QWidget 键盘主线
   - 后续若要支持，按特例方案单独设计
43. 旧键盘清理口径已收口：
   - `ui/keyboarddialog.ui` 属于历史 dialog 键盘方案，已不再属于当前正式主线
   - 当前正式主线不再依赖这份历史 UI 壳
44. 旧 Qt Virtual Keyboard 过渡链已退出当前工程主构建：
   - `RK3568.pro` 不再装配 `resources/inputmethod.qrc`
   - `src/app/app.pri` 不再编译 `QtVirtualKeyboardProvider.*` 和 `QtVirtualKeyboardDock.*`
   - `InputMethodCoordinator.*` 仍保留在源码树中，作为历史评估壳与删除前条件参考
   - `resources/inputmethod.qrc` 与 `src/app/qml/InputPanelHost.qml` 已退出工程装配
   - `ui/mainwindow.ui` 的 `keyboardPage` 历史壳页已清理
45. 当前阶段对旧键盘 / 旧输入法链只保留历史说明：
   - 只读盘点
   - 文档口径固化
   - 不再把旧链带回主线设计、实现与评审
46. 日常维护视角当前已进一步收口：
   - `keyboarddialog.ui` 不再属于日常页面维护对象
   - 旧 Qt Virtual Keyboard 过渡链不再进入日常开发主视野
   - 只有在专项历史核对时，才需要重新查看这组文件
47. 本轮清理后的主线判断以当前自定义 QWidget 键盘为准：
   - 登录页与系统设置页继续走自定义键盘主线
   - 旧链仅作为历史占位和兼容壳保留
48. 当前可执行的下一步已切换为新主线维护：
   - 继续补自定义键盘体验细节
   - 继续完善页面级避让与字段判型
   - 不再把旧 Qt Virtual Keyboard 过渡链纳入主线推进

51. 2026-03-23 本地 HTTP 接票入口已收紧错误语义：
   - 错路径：`404`
   - 错方法：`405`
   - request line / `Content-Length` / 非法 JSON / `taskId` 为空：`400`
52. 2026-03-23 `TicketIngressService` 启动日志已改成打印真实配置：
   - `ticket/httpIngressHost`
   - `ticket/httpIngressPort`
   - `ticket/httpIngressPath`
53. 2026-03-23 `TicketIngressService` 当前不再“收到 POST 就 success=true”：
   - 业务成功由 `KeyManageController -> TicketStore` 的真实入池结果决定
   - 重复任务被业务忽略时，也会返回更准确的成功消息，而不是统一 `ticket accepted`
54. 2026-03-24 系统设置页已进一步收口为更接近设备产品的简化形态：
   - 基础配置页主按钮统一为 `保存并应用`
   - `baudRate / dataBits` 已降级为固定参数说明
   - `keySerialCombo / cardSerialCombo` 保持统一下拉选择器外观
55. 2026-03-24 主窗口底部状态栏已收口为更明确的设备态展示：
   - 公司名口径为 `珠海云望科技有限公司`
   - 底部当前用户显示直接读取登录用户名
   - 角色信息单独显示
56. 2026-03-24 服务日志策略已重新收回到更接近原产品的口径：
   - 默认 `service/forceRestartScript=false`
   - 服务健康时不重启脚本
   - 服务异常时，再按当前模式决定是否恢复
57. 2026-03-24 服务日志来源已收口为“真实服务侧日志优先”：
   - 优先 `service_startup.log`
   - Linux 板端继续识别 `/home/linaro/outage/target/kids/log/*.log`
   - Windows 本地不再默认拿前端 `app.log` 冒充服务日志
58. 2026-03-24 当前若手动开启强制重启脚本，运行时仍支持更激进的恢复口径：
   - Windows：识别不到进程名时仍可按 PID 尝试终止
   - Linux：`fuser` 不存在时会自动回退 `ss` 查 PID 再杀
   - 但这条路径默认关闭，仅作为应急恢复能力保留
59. 2026-03-24 已确认原产品 `AdapterService` 的 Linux 服务恢复链：
   - 前端原产品进程是 `/home/linaro/AdapterService/bin/AdapterService`
   - 后端恢复脚本是 `/home/linaro/outage/start.sh`
   - 脚本本身只显式处理 `8081`，随后直接 `cd /home/linaro/outage && java -jar outage-api.jar`
60. 2026-03-24 当前 Linux 板端服务日志口径已继续向原产品对齐：
   - 优先读取 `/home/linaro/outage/target/kids/log/info-当天.log`
   - 优先读取 `/home/linaro/outage/target/kids/log/error-当天.log`
   - 不再把前端 `run.log / app.log` 视为板端服务日志主来源
61. 2026-03-24 系统设置页的 `保存并应用` 按钮已补双层保障：
   - `.ui` 静态样式中直接可见，方便 Qt Designer 调整
   - `systempage.cpp` 运行时仍会再次显式设尺寸、样式与抬层级，提升板端稳定性
62. 2026-03-25 刷卡登录主链已按原产品口径接入第一版：
   - 登录页接入 `CardSerialSource`
   - Linux 板端默认读 `/dev/ttyS3`
   - 当前固定按 `9600 8-N-1` 打开读卡串口
   - 采用 8 字节定长帧流式解析，不按“一次 readyRead 就是一条消息”处理
63. 2026-03-25 原产品抓包真值已并入当前实现：
   - 刷卡帧示例：`AA 31 08 00 49 A7 E0 06`
   - 实际上送后端的 `cardNo` 为 `080049a7e006`
   - 实际请求体为 `{\"cardNo\":\"080049a7e006\",\"tenantId\":\"000000\"}`
   - 请求接口为 `POST /api/kids-outage/third-api/login-by-card`
64. 2026-03-25 刷卡登录成功后的状态传播继续复用密码登录主链：
   - `AuthService::saveToken()` 仍负责写入 token 与 `AppContext`
   - `LoginPage -> MainWindow` 的成功回调链保持不变
   - 顶部用户名、底部当前用户、页面跳转与权限刷新不需要单独重写
65. 2026-03-25 系统设置页“用户身份采集”开始接入真实采卡闭环：
   - 不再使用阻塞式“准备采卡”占位弹窗
   - 改为页内提示“选择用户 -> 刷卡 -> 保存卡号”
   - 采卡阶段复用与登录页相同的 `CardSerialSource`
66. 2026-03-25 用户身份采集页后端接口口径已收口：
   - 刷新用户默认回退到 `POST /list-user`
   - 保存卡号默认调用 `POST /update-card-no`
   - 保存请求体固定为 `{"userId":"...","cardNo":"..."}` 
67. 2026-03-25 用户身份采集页当前依赖后端返回 `userId`：
   - 若用户列表接口只返回账号字符串、不返回 `userId`
   - 页面会允许查看用户，但会阻止“采集卡号”落库，并明确提示原因
68. 2026-03-25 系统页 `list-user / update-card-no` 业务请求当前改为优先带登录 token：
   - 若已登录，按后端文档发送 `kids-auth: bearer <accessToken>`
   - 同时保留 `kids-requested-with: KidsHttpRequest`
   - 不再把 `Authorization: Basic` 或 `Blade-Auth` 当作系统页业务接口默认认证头
69. 2026-03-25 Linux 板端增加一轮读卡串口迁移：
   - 若旧配置仍残留 `/dev/ttyACM0`
   - 且板端存在 `/dev/ttyS3`
   - 程序会自动迁到 `/dev/ttyS3`
70. 2026-03-26 用户身份采集页已进一步按原产品权限边界收口：
   - `list-user` 对普通已登录角色可读
   - `update-card-no` 当前以后端权限为准，管理员可操作，审核等普通角色会被拒绝
   - 前端当前已同步改为：非管理员仅可查看用户列表，不可采集或删除卡号
71. 2026-03-26 用户身份采集页当前交互已收口为更安全的设备侧流程：
   - 表格固定显示垂直滚动条，适配 1024x768 板端
   - 指纹列默认摘要显示，完整值通过 tooltip 查看
   - 新增 `删除卡号` 按钮
   - 若刷到的卡已绑定其他用户，前端会直接提示“请先删除原绑定后再采集”，不再把后端 `400 Bad Request` 直接暴露给用户
72. 2026-03-26 用户身份采集页与原产品权限验证结果已固化：
   - `s1/audit` 等普通角色：`list-user` 可读，但 `update-card-no` 会被后端拒绝
   - `admin/administrator`：可执行 `删除卡号 / 采集卡号`
   - 前端当前已主动按此规则置灰非管理员操作按钮，减少无效操作
73. 2026-03-26 登录页已补第一版“服务就绪门禁”：
   - 登录页显示时会轻量探测 `list-account`
   - 只有探测返回正常后，才开放账号密码登录与刷卡登录
   - 服务未就绪时，页面会提示“服务启动中，请稍候”
74. 2026-03-26 用户身份采集页已补原产品风格占位按钮：
   - `删除指纹`
   - `采指纹`
   当前仅做静态展示与权限提示，功能尚未接入
75. 2026-03-26 当前产品口径已明确：
   - “服务日志页打开”不等于“后端服务已 ready”
   - 登录门禁以后端探测结果为准
   - 服务日志页只承担日志查看与服务侧状态观察
76. 2026-03-26 当前切换用户不会主动重启后端服务：
   - 主窗口切换用户只更新登录态与页面可访问状态
   - Linux 默认 `remote` 模式下，通常在首次显示日志页时启动日志控制器
   - 非 Linux `remote` 模式下，也可能在日志页构造阶段直接启动
   - 当前主链更偏向“检查服务可达 + 跟踪日志”，不是每次都重新执行本地启动脚本
77. 2026-03-26 底部硬件状态栏已开始从占位文案改为真实状态链：
   - 读卡器状态当前已接入 `CardSerialSource` 的状态变化
   - 当前可区分：`空闲 / 检测中 / 就绪 / 异常 / 未配置串口`
   - 指纹仪当前明确显示为 `未接入`
78. 2026-03-26 当前读卡器状态实现仍是“页内读卡源复用”方案：
   - 底部状态当前跟随登录页/系统设置页中的读卡源生命周期
   - 这样优先避免多个对象争抢 `/dev/ttyS3`
   - 尚未升级为独立常驻的全局硬件探测服务
79. 2026-03-26 登录页服务就绪探测已收口为异步轮询：
   - 不再在 GUI 线程里通过同步 `QEventLoop` 阻塞等待探测结果
   - 服务未就绪时仍保持门禁，但不会周期性拖慢登录页触摸与键盘交互
80. 2026-03-30 钥匙回传稳定性专项已拿到新一轮板端正向结果：
   - 工作台 JSON `accepted` 后，系统票可自动入池并进入自动传票
   - 本轮板端样本已观察到：首次自动传票超时后，可回退到 `auto-pending` 再自动重试
   - 后续已在板端成功收口到 `transferState=success`
81. 2026-03-30 删除验证链已完成板端专项验证：
   - 回传上传完成后，状态会进入 `return-delete-verifying`
   - 删除验证阶段若钥匙被拿走，当前不会直接判死
   - 重新放回并继续对账后，已可最终收口到 `return-delete-success`
82. 2026-03-30 多次快进快出压力场景已拿到板端正向证据：
   - 已拿到“多轮快进快出后最终恢复成功”的板端证据
   - 同一任务可在 `return-interrupted-retryable <-> return-handshake` 间多次来回恢复
   - 若执行者本轮操作确为 30 次，则该结果可作为 30 次压力场景材料
   - 最终稳定放回后，仍可继续进入 `return-log-requesting -> return-uploading -> return-delete-verifying -> return-delete-success`
83. 2026-03-30 当前仍未独立关项的重点只剩：
   - 工作台白屏恢复专项板端验证
   - 更底层协议事件级的 session 标记下沉
84. 2026-03-30 本轮专项结论、验证边界与临时日志处置，当前以本文件为主；`operations/KEY_RETURN_STABILITY_STATUS_2026-03-30.md` 保留为时点专项记录。
85. 2026-04-01 按任务单号独立回传第一轮代码已落地：
   - 手工回传已从“整把钥匙全部完成”切到“只看目标任务是否完成”
   - 自动回传已去掉“整把钥匙总门禁”，可从已完成任务中选择一条发起回传
   - 当前自动回传仍保持严格串行，不支持多任务并发回传
86. 2026-04-01 独立回传 UI / 控制器口径已做第一轮对齐：
   - 手工回传按钮当前直接复用控制器门禁，不再出现“页面可点、控制器拒绝”的已知错位
   - 当前产品约束已明确为：按任务独立回传，但同一时刻只允许一条回传链在途
   - 本轮结论为代码级落地，仍待板端真机验证闭环
87. 2026-04-03 `cancelTicket` 第一版稳定链已落地：
   - 本地 HTTP 入口已同时支持：
     - `/ywticket/WebApi/transTicket`
     - `/ywticket/WebApi/cancelTicket`
   - `cancelTicket` 当前已区分：
     - 本地无票
     - 已完成闭环无需撤销
     - 已在回传清理链中无需重复撤销
     - 未下发且确认不在钥匙中时直接移除
     - 其他情况受理为 `cancel-accepted/cancel-pending`
   - 当前已补：
     - `cancelState`
     - 最小撤销记录 `logs/cancel_ticket_journal.json`
     - 自动传票 / 自动回传 / 自动恢复对撤销票的冻结
     - `Q_TASK -> DEL -> Q_TASK verify` 的撤销删除闭环
88. 2026-04-03 撤销链当前仍需后续继续确认的重点：
   - 板端真机下 `cancel-pending -> cancel-executing -> cancel-done` 的完整样本
   - 程序重启后最小撤销记录恢复的现场验证
   - 多钥匙场景当前只预埋 `residentSlotId/residentKeyId`，尚未进入真实多钥匙调度

补充说明（2026-03-20）：

1. 旧 Qt Virtual Keyboard 过渡链已从当前工程主构建中退出。
2. `RK3568.pro` 已不再装配 `resources/inputmethod.qrc`。
3. `src/app/app.pri` 已不再编译 `QtVirtualKeyboardProvider.*` 和 `QtVirtualKeyboardDock.*`。
4. `InputMethodCoordinator.*` 仍保留在源码树中，作为历史评估壳与删除前条件参考。
5. `resources/inputmethod.qrc` 与 `src/app/qml/InputPanelHost.qml` 已退出工程装配。

## 3. 当前页面能力

### 钥匙管理页

1. `HTTP服务端报文`：可看到收到的 JSON 原文。  
2. `系统票数据`：可看到系统票列表。  
3. 左侧“当前选中票据”：跟随系统票选中项联动。  
4. `传票`：已接入正式发送入口。  

### 串口报文页

1. 仍由原有钥匙串口主链提供。  
2. 旧日志链没有被替换成平行实现。  

## 4. 已落地但尚未完全真机确认

1. 传票单帧 ACK 闭环的最终稳定性。  
2. 传票多帧 ACK 续发的最终稳定性。  
3. 自动传票在“钥匙已插但握手未完成”窗口中的最终稳定性。  
4. 自动传票在重复任务场景下的真实行为。  
5. 多帧 `UP_TASK_LOG` 回传的 ACK/续传规则在真实钥匙上的最终行为。  
6. `初始化 / 下载 RFID` 与不同业务样本（尤其送电任务）的结果一致性。  
7. 工作台撤销链已完成代码级第一版落地，但板端真机验证仍待继续补齐。  
8. replay 回归在当前会话内尚未真正执行：
   - 已补充多任务门禁场景脚本
   - 但尚未在可观测环境中实际运行并核对输出
9. 自定义键盘当前仍待继续收口的重点：
   - 系统设置页滚动避让精调
   - 中文候选体验细化
   - 工作台页后续特例方案

## 5. 当前明确未做 / 刻意延后

1. 多钥匙场景下撤销链的真实 slot 绑定与现场验证。  
2. 将 `INIT / 下载 RFID` 改造成 ready 自动前置链。  
4. 测试工具 UI（Replay / Diff / Golden）不并入主程序。  
5. `http_ticket_server` 独立 app 形态不并入主程序。  
6. `auth / rfid / stepId / stepType` 精细映射不在当前优先级。  
7. 工作台页特例键盘方案延后到本地页主线稳定后再推进。  

## 6. 下一步重点

1. 继续补 **送电任务** 与 **真实多帧回传** 样本。  
2. 先补齐后端 RFID 数据，再继续用真实锁验证回传。  
3. 在主链完整后，再统一处理：
   - 系统票删除/撤销
   - 重复下发高级规则
   - 其他业务逻辑 bug 收口
4. 继续补“换钥匙对照样本”和“真实多帧回传样本”，确认回传 `rfid` 差异是否与钥匙本身有关
5. 当前这轮还**没有**加真正的“通讯状态防抖定时器”。
   - 这轮优先修的是“本地系统票状态与钥匙真实状态脱节”问题，因为它会直接卡住自动回传与手动回传。
   - 如果后续验证发现顶部状态仍然抖得厉害，或底座一晃就频繁 `ready / not-ready`，下一步再单独补一层通讯状态防抖：
     - `ready` 连续稳定一段时间，才显示“钥匙已就绪”
     - 短暂掉线先显示“接触波动”，不立刻判死
   - 这一层很适合当前底座接触不稳的现场，但建议在“状态对账修复”验证完成后再加
6. 自定义键盘下一步重点不是“继续扩页面”，而是：
   - 继续收系统设置页局部避让
   - 继续收中文候选体验
   - 保持工作台页暂不并入主线

## 7. 当前已拍板且已落地的撤销业务规则

1. 工作台撤销一条**尚未传进钥匙**的系统票时：
   - 系统票可直接从活跃列表移除
2. 工作台撤销一条**已经传进钥匙**的系统票时：
   - 不抢占当前链路
   - 先受理为 `cancel-accepted/cancel-pending`
   - 再按 `Q_TASK -> DEL -> Q_TASK verify` 收口
   - 钥匙侧确认删除后再从活跃列表移除
3. 已完成回传闭环的票：
   - 直接返回“任务已完成，无需撤销”
4. 已处于 `return-upload-success / return-delete-pending / return-delete-verifying` 的票：
   - 不新起撤销链
   - 直接返回“任务已在清理中，无需重复撤销”
5. 当前正式入口仍是：
   - 新增工作台撤销事件输入
   - 不走“先加页面删除按钮”的旧思路

## 8. 联调补充结论（2026-03-09）

1. 当前 Windows 本地联调环境下，`localhost` 就是当前主程序所连的有效后端，不需要强制改成 `192.168.3.131`。  
2. 若浏览器访问 `http://localhost/api/kids-outage/third-api/finish-step-batch` 返回 `405`：
   - 说明接口存在
   - 只是浏览器发的是 `GET`
   - 主程序发 `POST` 时可正常工作
3. 对 414 锁出现的回传失败，经确认是：
   - 当前联调用后端缺少 414 的 RFID 数据
   - 后端无法将回传 `RFID` 匹配到闭锁装置
   - 不属于当前主程序回传 HTTP/串口解析主链错误
4. 原产品对同一把 411 锁的单步停电 / 单步送电样本也表现出：
   - 工作台步骤 `rfid` 固定为 `C9:04:99:A0:02`
   - 回传日志 `rfid` 分别出现 `C90499A001` / `C90499A000`
   - 因此当前主程序出现此类现象时，应优先视为“兼容原产品行为”
5. 钥匙管理页中的顶部 `Q_TASK诊断` 与表格旁 `读取钥匙票列表` 当前底层都走 `Q_TASK`，只是一个偏诊断入口、一个偏业务入口
6. `刷新本地系统票列表` 当前仅刷新主程序本地票池，不会重新向工作台/后端主动拉票
7. `transferState` 表示“主程序对传票下发状态的本地判断”，不是钥匙侧真值；当本地状态和钥匙真实任务状态冲突时，当前以 `Q_TASK` 结果做对账修正
8. 顶部“通讯”状态不再只看 `SET_COM ACK`；当前只有在握手完成后又拿到一次真实业务响应（例如 `Q_TASK / I_TASK_LOG / UP_TASK_LOG`）才视为“钥匙已就绪”

## 9. 2026-03-23 键盘口径补充

1. 当前页面能力已按产品边界收口：
   - 登录页 / 系统设置页：完整支持
   - 工作台页：条件支持（必须先聚焦网页输入元素）
   - 钥匙管理页：当前不作为有效输入页
   - 服务日志页：当前不支持
2. 工作台页当前已进入特例 bridge 阶段：
   - 不按普通 Widgets 输入页处理
   - 只有在网页当前焦点位于可编辑元素时，才允许打开并输入
3. 主窗口已增加跨页键盘收口：
   - 页面切换时统一收起键盘
   - 清空上一个页面的输入目标
   - 复位顶部键盘按钮状态
85. 2026-03-31 钥匙串口当前已选定新的产品执行口径：
   - 系统设置修改 `serial/keyPort` 后，仅保存配置，不在运行中切换串口
   - 当前现场工程师使用规则改为：`保存配置，重启程序后生效`
   - 读卡串口 `serial/cardPort` 当前同样按“保存配置，重启后生效”处理
   - `keyPort / cardPort` 当前仍保持独立，不做自动联动
86. 当前围绕串口配置的重点不再是热切换竞态，而是业务化重启边界：
   - 若存在传票、回传、删除验证、INIT、下载 RFID 等未收口链路，应提示“任务完成后再重启生效”
   - 后续若要进一步提升体验，可再评估“一键保存并重启”或热切换方案，但不作为当前主路径
