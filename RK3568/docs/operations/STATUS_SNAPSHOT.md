# 当前状态快照（STATUS SNAPSHOT）

Status: Active  
Owner: 项目维护者  
Last Updated: 2026-03-09  
适用范围：快速说明“项目当前做到哪里、哪些链路可用、哪些仍待真机验证”。  
不适用范围：不替代协议逐字节参考，不替代完整架构文档。  

## 1. 当前阶段一句话

RK3568 主程序已完成“工作台 JSON 进入主程序 -> 系统票入池 -> 钥匙管理页展示 -> 手动/自动传票接主链”的主链落位；当前重点已转向回传链路，业务 bug 收口后置。

## 2. 已完成

1. 主程序内已接入本地 HTTP 接收能力：
   - 默认 `127.0.0.1:8888`
   - 默认路径 `/ywticket/WebApi/transTicket`
   - 支持 `OPTIONS` 预检与跨域
2. 接收到的 JSON 会：
   - 落盘到 `logs/http_ticket_*.json`
   - 进入系统票池
   - 展示到钥匙管理页 `HTTP服务端报文`
   - 展示到钥匙管理页 `系统票数据`
3. 主程序内已经移植传票协议纯逻辑：
   - `JSON -> payload`
   - `payload -> frame(s)`
   - 单帧 / 多帧
   - ACK 驱动续发
4. 手动传票入口已改成：
   - 选中系统票
   - 点“传票”
   - 走主程序钥匙主链
5. 自动传票逻辑已落地，并已开启默认值：
   - `ticket/autoTransferEnabled=true`
   - 仍受串口连接、钥匙在位、钥匙稳定、`sessionReady` 共同约束
6. 同一 `taskId` 在 `sending/success` 状态下再次触发时，当前默认忽略重复下发
7. 当前真机联调已确认：
   - 工作台 JSON 接收正常
   - 系统票入池正常
   - 手动传票主链可进入正式发送入口
   - 自动传票主链已开始真机验证

## 3. 当前页面能力

### 钥匙管理页

1. `HTTP服务端报文`：可看到收到的 JSON 原文。  
2. `系统票数据`：可看到系统票列表。  
3. 左侧“当前选中票据”：跟随系统票选中项联动。  
4. `传票`：已接入正式发送入口。  

### 串口报文页

1. 仍由原有钥匙串口主链提供。  
2. 旧日志链没有被替换成平行实现。  

## 4. 已落地但尚未完全真机确认

1. 传票单帧 ACK 闭环的最终稳定性。  
2. 传票多帧 ACK 续发的最终稳定性。  
3. 自动传票在“钥匙已插但握手未完成”窗口中的最终稳定性。  
4. 自动传票在重复任务场景下的真实行为。  
5. 回传链路尚未正式接入主程序。  

## 5. 当前明确未做 / 刻意延后

1. 系统票删除/撤销业务规则。  
2. 回传链路主链。  
3. 初始化 / 下载 RFID / 回传 主链的完整收口。  
4. 测试工具 UI（Replay / Diff / Golden）不并入主程序。  
5. `http_ticket_server` 独立 app 形态不并入主程序。  
6. `auth / rfid / stepId / stepType` 精细映射不在当前优先级。  

## 6. 下一步重点

1. 继续做 **回传链路**。  
2. 再继续收 `初始化 / 下载 RFID / 回传` 这几条业务主链。  
3. 在主链完整后，再统一处理：
   - 系统票删除/撤销
   - 重复下发高级规则
   - 其他业务逻辑 bug 收口
